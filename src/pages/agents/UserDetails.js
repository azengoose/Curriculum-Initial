
export default function UserProfileDetails() {

    // Query for Profile Completed Data from matching user
    // OR use location state to pass data from UserProfile.js
    //  in terms of Progress data

    // Set state of Progress Data 

    return(
        <>
            <h3 className="theme-h3">Profile Details</h3>
            {/* <div>
            {Progress.map((e, i) => {
                return (
                <div key={i}>
                    {e.Title}
                    {e.Start_Time}
                </div>
                );
            })}
            </div> */}
</>
    )
}

      {/* {(displayName === name) ?
                <div>
                    This is you.
                    {email}
                    {uid} 
                </div> 
            : "This is not you."} */}